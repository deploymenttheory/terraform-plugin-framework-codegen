// Code generated by tfpfgen from blueprints/thousandeyes
// (sha256:d7d962a5ecfecf5cdb43ebecd751c3c49e8b7809021b4f9f802136d49a7c950b). DO NOT EDIT.

// The predicate below is the generated default: readable means consistent. The
// generated retry asks whether the object is *readable*, which every API
// answers the same way -- a 404 means not yet. An API that returns a *stale*
// object instead needs a stronger answer -- a modification timestamp, a
// revision counter, a status leaving "pending" -- and which field that is only
// probe evidence can say. When a recording establishes it, the fact belongs in
// the blueprint and this body is regenerated from it; an edit made here
// directly is drift the check reports. Removing hooks.readBackPredicate
// removes this file.

package tag

import (
	"context"

	"github.com/hashicorp/terraform-plugin-log/tflog"
)

// readBackLanded reports whether a write has taken effect in the object just read.
//
// Returning false asks the retry loop to read again; returning true accepts the state as
// mapped. Returning true unconditionally -- as this scaffold does -- reproduces the behaviour
// you would get with no predicate at all, so it is a safe starting point.
//
// state is the model as just populated from the API. Compare it against whatever your API
// touches on write.
func readBackLanded(ctx context.Context, state *TagResourceModel) bool {
	tflog.Trace(ctx, "read-back predicate not implemented, accepting the first read", map[string]any{
		"resource": TypeName,
	})

	return true
}
