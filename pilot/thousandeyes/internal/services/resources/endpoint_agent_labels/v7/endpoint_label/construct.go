// Code generated by tfpfgen from blueprints/thousandeyes
// (sha256:03f1a9d1deee1e76e51e48dbbc071404c0737e7a841d85bcad5a9c27643a1e84). DO NOT EDIT.

package endpoint_label

import (
	"context"

	"github.com/hashicorp/terraform-plugin-framework/diag"
	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/hashicorp/terraform-plugin-log/tflog"

	"github.com/deploymenttheory/go-sdk-thousandeyes/thousandeyes/thousandeyes_api/endpoint_agent_labels"
	"github.com/deploymenttheory/terraform-plugin-framework-codegen/pilot/thousandeyes/internal/services/common/convert"
)

// constructResource maps Terraform configuration onto the API's request body.
//
// Only writable attributes appear here. A computed attribute has no expand
// conversion, so it cannot be sent by construction rather than by discipline.
func constructResource(ctx context.Context, data *EndpointLabelResourceModel) (*endpoint_agent_labels.LabelRequest, diag.Diagnostics) {
	var (
		diags diag.Diagnostics
		d     diag.Diagnostics
	)
	tflog.Debug(ctx, "constructing request body", map[string]any{"resource": TypeName})

	body := &endpoint_agent_labels.LabelRequest{}

	body.Color = convert.FrameworkToPtrString(data.Color)
	body.Filters, d = expandEndpointLabelFilters(ctx, data.Filters)
	diags.Append(d...)
	body.MatchType = convert.FrameworkToEnum[endpoint_agent_labels.MatchType](data.MatchType)
	body.Name = convert.FrameworkToString(data.Name)

	return body, diags
}

// constructResourceUpdate builds the update request body.
//
// A separate function because this SDK gives update its own request type. The
// assignment list is create's except where an attribute declares updateExpand --
// an SDK whose update type spells a field differently from create's -- and the
// bindings check has proven every assigned field exists on its own type.
func constructResourceUpdate(ctx context.Context, data *EndpointLabelResourceModel) (*endpoint_agent_labels.Label, diag.Diagnostics) {
	var (
		diags diag.Diagnostics
		d     diag.Diagnostics
	)
	tflog.Debug(ctx, "constructing update request body", map[string]any{"resource": TypeName})

	body := &endpoint_agent_labels.Label{}

	body.Color = convert.FrameworkToPtrString(data.Color)
	body.Filters, d = expandEndpointLabelFilters(ctx, data.Filters)
	diags.Append(d...)
	body.MatchType = convert.FrameworkToEnum[endpoint_agent_labels.MatchType](data.MatchType)
	body.Name = convert.FrameworkToPtrString(data.Name)

	return body, diags
}

// expandEndpointLabelFilters converts the configured EndpointLabelFilterModel values into SDK structs.
func expandEndpointLabelFilters(ctx context.Context, v types.Set) ([]endpoint_agent_labels.Filter, diag.Diagnostics) {
	var diags diag.Diagnostics
	var d diag.Diagnostics

	// Null and unknown both yield nil so the field is omitted from the request,
	// which is distinct from an empty collection asking the API to clear it.
	if v.IsNull() || v.IsUnknown() {
		return nil, diags
	}

	var models []EndpointLabelFilterModel
	diags.Append(v.ElementsAs(ctx, &models, false)...)
	if diags.HasError() {
		return nil, diags
	}

	out := make([]endpoint_agent_labels.Filter, 0, len(models))
	for _, m := range models {
		var item endpoint_agent_labels.Filter

		item.Key = convert.FrameworkToEnum[endpoint_agent_labels.FilterType](m.Key)
		item.Mode = convert.FrameworkToEnum[endpoint_agent_labels.LabelFilterMode](m.Mode)
		item.Values, d = convert.FrameworkSetToStringSlice(ctx, m.Values)
		diags.Append(d...)
		out = append(out, item)
	}

	return out, diags
}
