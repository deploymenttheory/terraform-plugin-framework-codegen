// Code generated by tfpfgen from blueprints/jamfpro
// (sha256:e29c3c55fac9427f0859dfeab08822cf7cbca1463246f5d9cdeb8393ab9d2a4c). DO NOT EDIT.

package v2_inventory_preload_record_test

import (
	"os"
	"path/filepath"
	"testing"
	"time"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"

	"github.com/deploymenttheory/terraform-plugin-framework-codegen/pilot/jamfpro/internal/acceptance"
	"github.com/deploymenttheory/terraform-plugin-framework-codegen/pilot/jamfpro/internal/acceptance/check"
	"github.com/deploymenttheory/terraform-plugin-framework-codegen/pilot/jamfpro/internal/acceptance/destroy"
	"github.com/deploymenttheory/terraform-plugin-framework-codegen/pilot/jamfpro/internal/services/datasources/inventory_preload/v7/v2_inventory_preload_record"
)

// The data source under test, and the seed resource whose apply produced the object it
// reads. The only thing this test creates is the seed, so existence and destroy are
// verified through the seed's own helper.
const (
	address     = "data.jamfpro_v2_inventory_preload_record.test"
	seedAddress = "jamfpro_v2_inventory_preload_record.test"
)

var seedResource = v2_inventory_preload_record.V2InventoryPreloadRecordTestResource{}

// TestAccDataSourceV2InventoryPreloadRecord_01_Read seeds one object and reads it back through the data source.
//
// Every cross-key check compares the data source's state against the state the seed's
// apply produced. That is the point of the test: the two arrive by different code paths
// -- the resource's create-then-read against the data source's read -- and a flatten
// that drops or mangles a field on either path shows up here as a mismatch, which no
// amount of compiling can see.
func TestAccDataSourceV2InventoryPreloadRecord_01_Read(t *testing.T) {
	resource.Test(t, resource.TestCase{
		PreCheck:                 func() { acceptance.PreCheck(t) },
		ProtoV6ProviderFactories: acceptance.ProtoV6ProviderFactories,
		// hashicorp/time supplies the propagation wait the fixture declares between
		// creating the seed and reading it back.
		ExternalProviders: map[string]resource.ExternalProvider{
			"time": {Source: "hashicorp/time", VersionConstraint: ">= 0.14.0"},
		},
		// The data source owns nothing; destroy verification is the seed's.
		//
		// The wait is the built-in default; this API's consistency after delete was never measured,
		// so this is a budget rather than an observation
		CheckDestroy: destroy.CheckDestroyed(seedResource, "jamfpro_v2_inventory_preload_record", 20*time.Second),
		Steps: []resource.TestStep{
			{
				Config: config(t, "datasource.tf"),
				Check: resource.ComposeAggregateTestCheckFunc(
					check.That(address).Key("apple_care_id").MatchesOtherKey(check.That(seedAddress).Key("apple_care_id")),
					check.That(address).Key("asset_tag").MatchesOtherKey(check.That(seedAddress).Key("asset_tag")),
					check.That(address).Key("bar_code1").MatchesOtherKey(check.That(seedAddress).Key("bar_code1")),
					check.That(address).Key("bar_code2").MatchesOtherKey(check.That(seedAddress).Key("bar_code2")),
					check.That(address).Key("building").MatchesOtherKey(check.That(seedAddress).Key("building")),
					check.That(address).Key("department").MatchesOtherKey(check.That(seedAddress).Key("department")),
					check.That(address).Key("device_type").MatchesOtherKey(check.That(seedAddress).Key("device_type")),
					check.That(address).Key("email_address").MatchesOtherKey(check.That(seedAddress).Key("email_address")),
					check.That(address).Key("full_name").MatchesOtherKey(check.That(seedAddress).Key("full_name")),
					check.That(address).Key("id").MatchesOtherKey(check.That(seedAddress).Key("id")),
					check.That(address).Key("lease_expiration").MatchesOtherKey(check.That(seedAddress).Key("lease_expiration")),
					check.That(address).Key("life_expectancy").MatchesOtherKey(check.That(seedAddress).Key("life_expectancy")),
					check.That(address).Key("phone_number").MatchesOtherKey(check.That(seedAddress).Key("phone_number")),
					check.That(address).Key("po_date").MatchesOtherKey(check.That(seedAddress).Key("po_date")),
					check.That(address).Key("po_number").MatchesOtherKey(check.That(seedAddress).Key("po_number")),
					check.That(address).Key("position").MatchesOtherKey(check.That(seedAddress).Key("position")),
					check.That(address).Key("purchase_price").MatchesOtherKey(check.That(seedAddress).Key("purchase_price")),
					check.That(address).Key("purchasing_account").MatchesOtherKey(check.That(seedAddress).Key("purchasing_account")),
					check.That(address).Key("purchasing_contact").MatchesOtherKey(check.That(seedAddress).Key("purchasing_contact")),
					check.That(address).Key("room").MatchesOtherKey(check.That(seedAddress).Key("room")),
					check.That(address).Key("serial_number").MatchesOtherKey(check.That(seedAddress).Key("serial_number")),
					check.That(address).Key("username").MatchesOtherKey(check.That(seedAddress).Key("username")),
					check.That(address).Key("vendor").MatchesOtherKey(check.That(seedAddress).Key("vendor")),
					check.That(address).Key("warranty_expiration").MatchesOtherKey(check.That(seedAddress).Key("warranty_expiration")),
				),
			},
		},
	})
}

// config reads the committed fixture; see the resource acceptance test for why testdata.
func config(t *testing.T, name string) string {
	t.Helper()

	body, err := os.ReadFile(filepath.Join("testdata", name))
	if err != nil {
		t.Fatalf("reading fixture %s: %v", name, err)
	}

	return string(body)
}
