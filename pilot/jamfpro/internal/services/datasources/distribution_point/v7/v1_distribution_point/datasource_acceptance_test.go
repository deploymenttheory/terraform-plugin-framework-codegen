// Code generated by tfpfgen from blueprints/jamfpro
// (sha256:e29c3c55fac9427f0859dfeab08822cf7cbca1463246f5d9cdeb8393ab9d2a4c). DO NOT EDIT.

package v1_distribution_point_test

import (
	"os"
	"path/filepath"
	"testing"
	"time"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"

	"github.com/deploymenttheory/terraform-plugin-framework-codegen/pilot/jamfpro/internal/acceptance"
	"github.com/deploymenttheory/terraform-plugin-framework-codegen/pilot/jamfpro/internal/acceptance/check"
	"github.com/deploymenttheory/terraform-plugin-framework-codegen/pilot/jamfpro/internal/acceptance/destroy"
	"github.com/deploymenttheory/terraform-plugin-framework-codegen/pilot/jamfpro/internal/services/datasources/distribution_point/v7/v1_distribution_point"
)

// The data source under test, and the seed resource whose apply produced the object it
// reads. The only thing this test creates is the seed, so existence and destroy are
// verified through the seed's own helper.
const (
	address     = "data.jamfpro_v1_distribution_point.test"
	seedAddress = "jamfpro_v1_distribution_point.test"
)

var seedResource = v1_distribution_point.V1DistributionPointTestResource{}

// TestAccDataSourceV1DistributionPoint_01_Read seeds one object and reads it back through the data source.
//
// Every cross-key check compares the data source's state against the state the seed's
// apply produced. That is the point of the test: the two arrive by different code paths
// -- the resource's create-then-read against the data source's read -- and a flatten
// that drops or mangles a field on either path shows up here as a mismatch, which no
// amount of compiling can see.
func TestAccDataSourceV1DistributionPoint_01_Read(t *testing.T) {
	resource.Test(t, resource.TestCase{
		PreCheck:                 func() { acceptance.PreCheck(t) },
		ProtoV6ProviderFactories: acceptance.ProtoV6ProviderFactories,
		// hashicorp/time supplies the propagation wait the fixture declares between
		// creating the seed and reading it back.
		ExternalProviders: map[string]resource.ExternalProvider{
			"time": {Source: "hashicorp/time", VersionConstraint: ">= 0.14.0"},
		},
		// The data source owns nothing; destroy verification is the seed's.
		//
		// The wait is the built-in default; this API's consistency after delete was never measured,
		// so this is a budget rather than an observation
		CheckDestroy: destroy.CheckDestroyed(seedResource, "jamfpro_v1_distribution_point", 20*time.Second),
		Steps: []resource.TestStep{
			{
				Config: config(t, "datasource.tf"),
				Check: resource.ComposeAggregateTestCheckFunc(
					check.That(address).Key("backup_distribution_point_id").MatchesOtherKey(check.That(seedAddress).Key("backup_distribution_point_id")),
					check.That(address).Key("enable_load_balancing").MatchesOtherKey(check.That(seedAddress).Key("enable_load_balancing")),
					check.That(address).Key("file_sharing_connection_type").MatchesOtherKey(check.That(seedAddress).Key("file_sharing_connection_type")),
					check.That(address).Key("https_context").MatchesOtherKey(check.That(seedAddress).Key("https_context")),
					check.That(address).Key("https_enabled").MatchesOtherKey(check.That(seedAddress).Key("https_enabled")),
					check.That(address).Key("https_password").MatchesOtherKey(check.That(seedAddress).Key("https_password")),
					check.That(address).Key("https_port").MatchesOtherKey(check.That(seedAddress).Key("https_port")),
					check.That(address).Key("https_security_type").MatchesOtherKey(check.That(seedAddress).Key("https_security_type")),
					check.That(address).Key("https_username").MatchesOtherKey(check.That(seedAddress).Key("https_username")),
					check.That(address).Key("id").MatchesOtherKey(check.That(seedAddress).Key("id")),
					check.That(address).Key("local_path_to_share").MatchesOtherKey(check.That(seedAddress).Key("local_path_to_share")),
					check.That(address).Key("name").MatchesOtherKey(check.That(seedAddress).Key("name")),
					check.That(address).Key("port").MatchesOtherKey(check.That(seedAddress).Key("port")),
					check.That(address).Key("principal").MatchesOtherKey(check.That(seedAddress).Key("principal")),
					check.That(address).Key("read_only_password").MatchesOtherKey(check.That(seedAddress).Key("read_only_password")),
					check.That(address).Key("read_only_username").MatchesOtherKey(check.That(seedAddress).Key("read_only_username")),
					check.That(address).Key("read_write_password").MatchesOtherKey(check.That(seedAddress).Key("read_write_password")),
					check.That(address).Key("read_write_username").MatchesOtherKey(check.That(seedAddress).Key("read_write_username")),
					check.That(address).Key("server_name").MatchesOtherKey(check.That(seedAddress).Key("server_name")),
					check.That(address).Key("share_name").MatchesOtherKey(check.That(seedAddress).Key("share_name")),
					check.That(address).Key("ssh_password").MatchesOtherKey(check.That(seedAddress).Key("ssh_password")),
					check.That(address).Key("ssh_username").MatchesOtherKey(check.That(seedAddress).Key("ssh_username")),
					check.That(address).Key("workgroup").MatchesOtherKey(check.That(seedAddress).Key("workgroup")),
				),
			},
		},
	})
}

// config reads the committed fixture; see the resource acceptance test for why testdata.
func config(t *testing.T, name string) string {
	t.Helper()

	body, err := os.ReadFile(filepath.Join("testdata", name))
	if err != nil {
		t.Fatalf("reading fixture %s: %v", name, err)
	}

	return string(body)
}
