// Code generated by tfpfgen from blueprints/jamfpro
// (sha256:92588417fa010d48e3954c780a1c7284689923c40fd60ea7464f85cbc75b43eb). DO NOT EDIT.

package v1_script_test

import (
	"os"
	"path/filepath"
	"testing"
	"time"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"

	"github.com/deploymenttheory/terraform-plugin-framework-codegen/pilot/jamfpro/internal/acceptance"
	"github.com/deploymenttheory/terraform-plugin-framework-codegen/pilot/jamfpro/internal/acceptance/check"
	"github.com/deploymenttheory/terraform-plugin-framework-codegen/pilot/jamfpro/internal/acceptance/destroy"
	"github.com/deploymenttheory/terraform-plugin-framework-codegen/pilot/jamfpro/internal/services/datasources/scripts/v7/v1_script"
)

// The data source under test, and the seed resource whose apply produced the object it
// reads. The only thing this test creates is the seed, so existence and destroy are
// verified through the seed's own helper.
const (
	address     = "data.jamfpro_v1_script.test"
	seedAddress = "jamfpro_v1_script.test"
)

var seedResource = v1_script.V1ScriptTestResource{}

// TestAccDataSourceV1Script_01_Read seeds one object and reads it back through the data source.
//
// Every cross-key check compares the data source's state against the state the seed's
// apply produced. That is the point of the test: the two arrive by different code paths
// -- the resource's create-then-read against the data source's read -- and a flatten
// that drops or mangles a field on either path shows up here as a mismatch, which no
// amount of compiling can see.
func TestAccDataSourceV1Script_01_Read(t *testing.T) {
	resource.Test(t, resource.TestCase{
		PreCheck:                 func() { acceptance.PreCheck(t) },
		ProtoV6ProviderFactories: acceptance.ProtoV6ProviderFactories,
		// hashicorp/time supplies the propagation wait the fixture declares between
		// creating the seed and reading it back.
		ExternalProviders: map[string]resource.ExternalProvider{
			"time": {Source: "hashicorp/time", VersionConstraint: ">= 0.14.0"},
		},
		// The data source owns nothing; destroy verification is the seed's.
		//
		// The wait is the built-in default; this API's consistency after delete was never measured,
		// so this is a budget rather than an observation
		CheckDestroy: destroy.CheckDestroyed(seedResource, "jamfpro_v1_script", 20*time.Second),
		Steps: []resource.TestStep{
			{
				Config: config(t, "datasource.tf"),
				Check: resource.ComposeAggregateTestCheckFunc(
					check.That(address).Key("category_id").MatchesOtherKey(check.That(seedAddress).Key("category_id")),
					check.That(address).Key("category_name").MatchesOtherKey(check.That(seedAddress).Key("category_name")),
					check.That(address).Key("id").MatchesOtherKey(check.That(seedAddress).Key("id")),
					check.That(address).Key("info").MatchesOtherKey(check.That(seedAddress).Key("info")),
					check.That(address).Key("name").MatchesOtherKey(check.That(seedAddress).Key("name")),
					check.That(address).Key("notes").MatchesOtherKey(check.That(seedAddress).Key("notes")),
					check.That(address).Key("os_requirements").MatchesOtherKey(check.That(seedAddress).Key("os_requirements")),
					check.That(address).Key("parameter10").MatchesOtherKey(check.That(seedAddress).Key("parameter10")),
					check.That(address).Key("parameter11").MatchesOtherKey(check.That(seedAddress).Key("parameter11")),
					check.That(address).Key("parameter4").MatchesOtherKey(check.That(seedAddress).Key("parameter4")),
					check.That(address).Key("parameter5").MatchesOtherKey(check.That(seedAddress).Key("parameter5")),
					check.That(address).Key("parameter6").MatchesOtherKey(check.That(seedAddress).Key("parameter6")),
					check.That(address).Key("parameter7").MatchesOtherKey(check.That(seedAddress).Key("parameter7")),
					check.That(address).Key("parameter8").MatchesOtherKey(check.That(seedAddress).Key("parameter8")),
					check.That(address).Key("parameter9").MatchesOtherKey(check.That(seedAddress).Key("parameter9")),
					check.That(address).Key("priority").MatchesOtherKey(check.That(seedAddress).Key("priority")),
					check.That(address).Key("script_contents").MatchesOtherKey(check.That(seedAddress).Key("script_contents")),
				),
			},
		},
	})
}

// config reads the committed fixture; see the resource acceptance test for why testdata.
func config(t *testing.T, name string) string {
	t.Helper()

	body, err := os.ReadFile(filepath.Join("testdata", name))
	if err != nil {
		t.Fatalf("reading fixture %s: %v", name, err)
	}

	return string(body)
}
