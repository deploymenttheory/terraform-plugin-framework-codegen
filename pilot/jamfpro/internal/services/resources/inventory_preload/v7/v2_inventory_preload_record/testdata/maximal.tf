# Code generated by tfpfgen from blueprints/jamfpro
# (sha256:92588417fa010d48e3954c780a1c7284689923c40fd60ea7464f85cbc75b43eb). DO NOT EDIT.

# The widest configuration the evidence supports: every writable attribute with a derivable
# value, chosen by the same derivation the rehearsal probe sends to the live API.
#
# Regenerated and policed, deliberately. This file used to be a hand-owned scaffold, and
# hand edits are how its values drifted from the generated assertions -- the checks derive
# from the blueprint, so the blueprint is where a value is corrected: an accFixture hint for
# something only a human knows, recorded behaviour for everything the probe observed. What
# could not be derived is listed inside the block, each with the reason.

resource "jamfpro_v2_inventory_preload_record" "test" {
  #
  # Not filled in, because no correct value could be derived. Each of these is optional, so this
  # configuration is valid as it stands -- but it is testing less than it could.
  #
  # apple_care_id: it references an object that must already exist, which a synthesised value
  # cannot name; supply one through accFixture if the test has the object
  #
  # extension_attributes: a nested object's members may be identifiers of objects that must
  # already exist, and nothing in the specification says whether they are
  #
  # username: it is credential-shaped, and a generated fixture must not invent values that read
  # as secrets; supply one through accFixture if the test needs it
  asset_tag           = "tfacc-asset-tag"
  bar_code1           = "tfacc-bar-code1"
  bar_code2           = "tfacc-bar-code2"
  building            = "tfacc-building"
  department          = "tfacc-department"
  device_type         = "tfacc-device-type"
  email_address       = "tfacc-email-address"
  full_name           = "tfacc-full-name"
  lease_expiration    = "tfacc-lease-expiration"
  life_expectancy     = "tfacc-life-expectancy"
  phone_number        = "tfacc-phone-number"
  po_date             = "tfacc-po-date"
  po_number           = "tfacc-po-number"
  position            = "tfacc-position"
  purchase_price      = "tfacc-purchase-price"
  purchasing_account  = "tfacc-purchasing-account"
  purchasing_contact  = "tfacc-purchasing-contact"
  room                = "tfacc-room"
  serial_number       = "tfacc-serial-number"
  vendor              = "tfacc-vendor"
  warranty_expiration = "tfacc-warranty-expiration"
}
