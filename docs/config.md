# tfpfgen.yaml reference

<!-- Generated by internal/config.Reference — do not edit by hand.
     Regenerate: UPDATE_DOCS=1 go test ./internal/config -run TestUnit_Config_ReferenceMatchesDocs -->

The schema is owned by `internal/config`; this page is generated from it, so every key below is one the decoder reads. Unknown keys fail the decode with a did-you-mean suggestion. The current schema version is 1.

## Top-level keys

| Key | Type | Default | Description |
|---|---|---|---|
| `version` | integer | — | Schema version of this file; must equal the schema version the installed toolkit understands. |

## `provider`

| Key | Type | Default | Description |
|---|---|---|---|
| `provider.name` | string | — | Provider name as published to the registry; a lowercase DNS label. |
| `provider.registry_namespace` | string | — | Registry namespace the provider is published under; a lowercase DNS label. |

## `generator`

| Key | Type | Default | Description |
|---|---|---|---|
| `generator.version` | string | — | Exact toolkit release tag (vX.Y.Z) the pipeline installs; branch names are refused. |

## `spec`

| Key | Type | Default | Description |
|---|---|---|---|
| `spec.document_url` | string | — | http(s) URL of the upstream OpenAPI document that `tfpfgen spec import` pins by hash. |

## `sdk`

| Key | Type | Default | Description |
|---|---|---|---|
| `sdk.backend` | string | — | SDK generator behind the common interface; exactly one per provider repo. |
| `sdk.backend_version` | string | — | Exact version pin of the backend tool to install. |
| `sdk.client_type_name` | string | `"APIClient"` | Go type name of the generated SDK client. |
| `sdk.include_paths` | list of string | — | Spec paths to include in SDK generation; empty means every path. |
| `sdk.exclude_paths` | list of string | — | Spec paths to exclude from SDK generation. |

`sdk.backend` is a closed set: `kiota`, `openapi-generator`.

## `auth`

| Key | Type | Default | Description |
|---|---|---|---|
| `auth.method` | string | — | How the audit authenticates against the live API; decides which secrets are required. |
| `auth.api_key_header` | string | — | Header name the API key is sent in; required by and only meaningful for `api_key_header`. |
| `auth.token_url` | string | — | OAuth2 token endpoint; required when the method is `oauth2_client_credentials`. |

`auth.method` is a closed set: `bearer_token`, `api_key_header`, `basic`, `oauth2_client_credentials`, `github_app`.

## `audit`

| Key | Type | Default | Description |
|---|---|---|---|
| `audit.enabled` | boolean | `true` | Whether the credentialed live-API audit stage runs at all. |
| `audit.base_url_override` | string | — | Base URL the audit calls instead of the server named in the spec. |
| `audit.name_prefix` | string | `"tfpfgen"` | Prefix every live object the audit creates carries; cleanup matches on it. |
| `audit.max_objects` | integer | `25` | Budget of live objects one audit run may create. |
| `audit.rate_limit_rps` | integer | `2` | Requests per second the audit may send to the live API. |
| `audit.auto_accept` | list of string | — | Observation kinds whose compiled corrections `tfpfgen spec revise` accepts without waiting for a human. |

Each `audit.auto_accept` entry is one of: `deleteNotFoundOK`, `dependsOn`, `derivedDefault`, `ignoredOnUpdate`, `immutable`, `listPagination`, `listWrapper`, `mutuallyExclusive`, `normalisation`, `readAfterWrite`, `requiredByAPI`, `requiredWhen`, `serverDefault`, `serverForced`, `undocumentedFieldInSpec`, `updateStyle`, `validConfiguration`, `validWhen`, `values`, `volatile`, `writable`.

## `services`

| Key | Type | Default | Description |
|---|---|---|---|
| `services.exclude` | list of string | — | Spec entities that become no provider code. |

## Secrets

Secret values never appear in tfpfgen.yaml. Each auth method reads a fixed set of environment variables — the names are a contract, not configuration:

| `auth.method` | Required secrets |
|---|---|
| `bearer_token` | `TFPFGEN_AUTH_TOKEN` |
| `api_key_header` | `TFPFGEN_AUTH_TOKEN` |
| `basic` | `TFPFGEN_AUTH_USERNAME`, `TFPFGEN_AUTH_PASSWORD` |
| `oauth2_client_credentials` | `TFPFGEN_AUTH_CLIENT_ID`, `TFPFGEN_AUTH_CLIENT_SECRET` |
| `github_app` | `TFPFGEN_AUTH_APP_ID`, `TFPFGEN_AUTH_APP_PRIVATE_KEY` |
