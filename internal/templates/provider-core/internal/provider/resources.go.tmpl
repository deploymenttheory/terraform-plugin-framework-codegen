{{ template "header" . }}

package provider

import (
	"context"

	"github.com/hashicorp/terraform-plugin-framework/resource"
	// tfpfgen:resources:imports
)

// Resources returns every managed resource this provider serves. The list
// is machine-maintained: `provider generate` appends one registration per
// generated resource at each sentinel comment and never edits other lines.
func (p *Provider) Resources(_ context.Context) []func() resource.Resource {
	return []func() resource.Resource{
		// tfpfgen:resources:registrations
	}
}
