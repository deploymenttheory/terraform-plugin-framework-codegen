{{ template "header" . }}

package {{ .Package }}

{{ .ValidatorImports }}

var _ resource.ResourceWithConfigValidators = (*{{ .Type }})(nil)

// ConfigValidators returns the cross-attribute validators the document
// declares: stock validators for the mechanical edges and named custom
// validators for the value-conditional ones the framework has no stock
// idiom for.
func (r *{{ .Type }}) ConfigValidators(ctx context.Context) []resource.ConfigValidator {
	return []resource.ConfigValidator{
{{ .ConfigValidatorExprs }}	}
}

{{ .CustomValidatorDecls }}
