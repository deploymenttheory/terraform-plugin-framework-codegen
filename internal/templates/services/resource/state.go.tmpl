{{ template "header" . }}

package {{ .Package }}

{{ .StateImports }}

// MapRemoteStateToTerraform maps the API's answer onto the framework
// model. A nil field in the answer becomes a null attribute.
func MapRemoteStateToTerraform(ctx context.Context, data *{{ .Type }}Model, remote {{ .ReadModel }}) {
	_ = ctx
	if remote == nil {
		return
	}
{{ .StateBody }}}
