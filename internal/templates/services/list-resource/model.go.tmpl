{{ template "header" . }}

package {{ .Package }}

import (
	"github.com/hashicorp/terraform-plugin-framework/types"
)

// identityModel is what names one listed instance remotely. It mirrors the
// {{ .TerraformType }} resource's identity schema, which is the shape
// terraform reads a list result against.
type identityModel struct {
{{ .IdentityFields }}}
{{ if .ConfigModel }}
// listConfigModel mirrors the list block's configuration.
{{ .ConfigModel }}
{{- end }}
