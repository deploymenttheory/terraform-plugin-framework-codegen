{{ template "header" . }}

package {{ .Package }}

import (
	"github.com/hashicorp/terraform-plugin-framework/types"
)

// identityModel is the identity every listed instance publishes.
type identityModel struct {
	ID types.String `tfsdk:"id"`
}
